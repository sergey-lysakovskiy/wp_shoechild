jQuery(document).ready(function($){
    /*var $container = $('.entries-wrapper');
    // init
    $container.isotope({
        // options
        itemSelector: 'article',
        layoutMode: 'fitRows'
    });*/    
})